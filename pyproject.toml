[project]
name = "bot-vercel"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.139.2",
    "python-telegram-bot>=22.8",
    "firebase-admin",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.51.0"
]

[tool.vercel]
entrypoint = "api.index:app"